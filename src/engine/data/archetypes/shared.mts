export const scaledScreen = levelData({
    l: Number,
    r: Number,
    b: Number,
    t: Number,

    w: Number,
    h: Number,

    wToH: Number,
})

export const flick = levelData({
    distance: Number,
})
